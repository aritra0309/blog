{
  "name": "project-log",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve dist"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  },
  "dependencies": {
    "katex": "^0.18.1"
  }
}
